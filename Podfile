# Define the global platform for your project
platform :ios, '15.0'

target 'eyespy' do
  use_frameworks!

  # Just need MediaPipe Vision
  pod 'MediaPipeTasksVision'
  
  # Testing targets
  target 'eyespyTests' do
    inherit! :search_paths
    # Pods for testing (if any needed, add here)
  end

  target 'eyespyUITests' do
    # Pods for UI testing (if any needed, add here)
  end
end